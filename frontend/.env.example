# Optional: point the Vite dev server proxy at a different backend URL.
VITE_API_PROXY_TARGET=http://localhost:5000

# Optional: when serving the built SPA against an absolute backend URL.
# VITE_API_BASE_URL=http://localhost:5000
